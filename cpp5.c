#include<stdio.h>
void main()
{	char ct;
	int it;
	float ft;
	double dt;
	printf("size of character type is %ld\n",sizeof(ct));
	printf("size of integer type is %ld\n",sizeof(it));
	printf("size of float type is %ld\n",sizeof(ft));
	printf("size of double type is %ld\n",sizeof(dt));
}
